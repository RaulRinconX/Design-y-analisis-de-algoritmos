INSTRUCCIONES PARA CORRER EL PROYECTO DE LA TAREA 5:

El proyecto fue creado y desarrollado en Intellij por lo que sí usted es usuario de Vscode o Intellij no deberá tener problema. Basta con importar el proyecto y escoger la carpeta "Kruskal" como modulo y descargar el SDK coretto 11.

En el proyecto se encontrara con la carpeta data, donde estan ubicados todos los datos de prueba que se usaron para probar el correcto funcionamiento de los algoritmos. Para los algoritmos Kruskal y Primm se le deben pasar los archivos .txt que tienen el siguiente formato: "undirectedpesosx" siendo x el tamaño de vertices que tiene ese grafo no dirigdo. Para el algoritmo de MaxFlow se pueden usar los demás archivos suministrados en la tarea pasada.

También se encontrara con la carpeta src>main desde donde puede correr cada main ; "kruskal","Prim","MaxFlow".

En cada ejecución la consola tiene prints de que es lo que requiere el programa para correr.

Happy grading :)